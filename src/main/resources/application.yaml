# application.yml - Cambodia E-Menu Platform Configuration
spring:
  application:
    name: cambodia-emenu-platform
  profiles:
    active: dev

  # Database Configuration
  datasource:
    url: jdbc:postgresql://152.42.219.13:5432/e_menu_platform
    username: admin
    password: Hour1819
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update # Keep update for both base and dev
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        show_sql: false
        use_sql_comments: false

  # Cache Configuration
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=1h

# Server Configuration
server:
  port: 8080
  url: "http://152.42.219.13:8080"
  servlet:
    context-path: /

# JWT Configuration
jwt:
  secret: da2bd42bef8f9f957d647ed13571d35b59c7055fb723eacdb05154878da17d77d3180385fc232cab38bded0fc7dbda0d439cc275e91164ade266d64d50633bc7
  expiration: 99999999999999999 # 24 hours

# API Documentation
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized

# Cambodia-Specific Configuration
cambodia:
  timezone: "Asia/Phnom_Penh"
  currency: "USD"
  language: "km"
  country: "KH"

# 🔔 ADD THIS SECTION: Telegram Configuration
telegram:
  bot:
    token: "8464259107:AAHdIC3EZb_3uID4vnhteY6PYtmO9WGDBjU"
    enabled: true
    chat-id: "1898032377"
    mock: false  # 🧪 ENABLE MOCK MODE
    webhook-url: ""
    polling: true
    timeout: 30
    retry-attempts: 3

  notifications:
    enabled: true
    product-created: true

  templates:
    product-created: |
      🆕 *New Product Created!*
      
      📱 *Product:* {productName}
      🏪 *Business:* {businessName}
      💰 *Price:* ${price}
      📂 *Category:* {categoryName}
      👤 *Created by:* {createdBy}
      📅 *Date:* {createdAt}
      
      🔗 View: {productUrl}

    user-registered: |
      👤 *New User Registered!*
      
      📧 *Email:* {email}
      👤 *Name:* {fullName}
      🏷️ *Type:* {userType}
      📅 *Date:* {registeredAt}

    business-registered: |
      🏪 *New Business Registered!*
      
      🏢 *Business:* {businessName}
      👤 *Owner:* {ownerName}
      📧 *Email:* {businessEmail}
      📞 *Phone:* {phoneNumber}
      🌐 *Subdomain:* {subdomain}
      📅 *Date:* {registeredAt}


# Application Configuration
app:
  name: "Cambodia E-Menu Platform"
  version: "1.0.0"
  description: "Restaurant Management Platform for Cambodia"

  # Data Initialization
  init:
    create-admin: true
    # ✅ UPDATED: Now used as userIdentifier for platform owner
    admin-email: "phatmenghor19@gmail.com"
    admin-password: "88889999"

  # CORS Configuration
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:4200,http://localhost:5173,http://152.42.219.13:8080,https://152.42.219.13:8080,http://152.42.219.13,https://152.42.219.13"
    allowed-methods: "GET,POST,PUT,DELETE,OPTIONS,PATCH"
    allowed-headers: "*"
    allow-credentials: true

  # Security Configuration
  security:
    token-cleanup:
      enabled: true
      schedule: "0 0 2 * * ?" # Daily at 2 AM
      retention-weeks: 1

  subdomain:
    base-domain: "menu.com"
    auto-create-enabled: true
    maintenance:
      enabled: true
      access-log-retention-days: 90
      expired-check-cron: "0 0 */6 * * ?" # Every 6 hours
      statistics-cron: "0 0 1 * * ?"       # Daily at 1 AM
      cleanup-cron: "0 0 2 * * SUN"        # Weekly on Sunday at 2 AM
    ssl:
      auto-enable: false
      certificate-provider: "letsencrypt"
    verification:
      required: false
      token-expiry-hours: 24

# Logging Configuration
logging:
  level:
    com.emenu: INFO
    com.emenu.config.DataInitializationService: DEBUG # Enable detailed logging for initialization
    com.emenu.security: INFO
    com.emenu.features.telegram: DEBUG # ADD THIS: Debug Telegram operations
    org.springframework.security: WARN
    org.springframework.web: WARN
    org.hibernate.SQL: OFF
    org.hibernate.type: OFF
    org.hibernate.engine: OFF
    org.hibernate.stat: OFF
    org.springframework.orm.jpa: WARN
    root: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/cambodia-emenu-platform.log
    max-size: 100MB
    max-history: 30

---
# Development Profile
spring:
  config:
    activate:
      on-profile: dev

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:your-email@gmail.com}
    password: ${EMAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: update # CHANGED: Use update instead of create-drop
    show-sql: false

logging:
  level:
    com.emenu: INFO
    com.emenu.config.DataInitializationService: DEBUG # Debug initialization
    com.emenu.security.jwt: DEBUG # Debug JWT operations
    com.emenu.features.telegram: DEBUG # ADD THIS: Debug Telegram in dev profile
    org.hibernate.SQL: OFF