package com.menghor.ksit.enumations;

/**
 * Position type enum for menu items
 */
public enum PositionType {
    TOP,        // Add at the top of the list
    BOTTOM,     // Add at the bottom of the list
    SPECIFIC,   // Add at the specific display order
    AFTER_ITEM  // Add after a specific menu item
}
